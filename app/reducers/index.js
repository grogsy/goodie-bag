// idea for visibility toggle:
//   dispatch actions that switch between views
import CandyList from "../components/listcandy";
import SingleCandy from "../components/singlecandy";

const initialState = {};

const rootReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default rootReducer;
